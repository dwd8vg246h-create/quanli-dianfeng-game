# 后台鉴权配置指南

> 目标：让"别人拿到链接就能进后台"这件事不再发生。
>
> 全程约 15 分钟，需要在**电脑**上操作（手机上装不了 CLI）。

---

## 先看清问题出在哪

现在后台有三道门，全是虚掩的：

| 门 | 现状 |
|---|---|
| 页面口令 | 校验跑在浏览器里，**第一个打开页面的人设什么口令就是什么** |
| 数据库密钥 | 写在页面源码里，任何人 F12 都能抄走 |
| 数据接口 | 拿着抄走的 key 直接调接口，**完全绕开页面和口令** |

所以只加"页面口令"没有用——**在浏览器里做鉴权，等于把锁装在门内的插销上**。

真正的解法只有一条：**把口令搬到服务端**。

---

## 架构改动

```
改前：浏览器 ──(携带 key)──► Supabase 数据库
                            任何人都能这么调

改后：浏览器 ──(只有口令)──► admin-guard 函数 ──(service_role)──► 数据库
                                   │
                                   └─ 口令在服务端比对，密钥不下发
```

---

## 第一步：建表（2 分钟）

打开 **Supabase → SQL Editor → New query**，粘贴 `安全加固_后台鉴权.sql` 全文，点 Run。

它做两件事：

1. 建 `admin_guard` 表（存失败次数与会话）
2. **收回** `admin_guard`、`admin_logs` 对匿名访客的全部权限

> ⚠️ 这一步之后，旧的"直连写日志"会失效——所以必须走完第二步，否则后台的操作留痕会写不进去。

---

## 第二步：部署函数（5 分钟）

### 方式 A：用 Dashboard（不需要装任何东西）

1. **Edge Functions → New function**
2. 名称填 `admin-guard`（**必须一字不差**，后面填的地址要用）
3. 把 `functions/admin-guard/index.ts` 全文粘贴进去
4. 点 **Deploy**

### 方式 B：用 CLI

```bash
supabase login
supabase link --project-ref zgkovjgkkvxoajcqxwfw
supabase functions deploy admin-guard
```

---

## 第三步：设口令（1 分钟）

**Project Settings → Edge Functions → Secrets → Add new secret**

| Name | Value |
|---|---|
| `ADMIN_TOKEN` | 你的管理口令（建议 12 位以上，字母+数字） |

可选：

| Name | Value | 作用 |
|---|---|---|
| `ADMIN_ORIGIN` | `https://dwd8vg246h-create.github.io` | 只认来自你自己站点的请求 |

> `SUPABASE_URL` 和 `SUPABASE_SERVICE_ROLE_KEY` 平台会自动注入，不用管。

**改完 Secrets 要重新部署一次函数**才生效（Dashboard 里点 Redeploy）。

---

## 第四步：后台启用（1 分钟）

打开后台 → 展开「🔌 连接配置」→ 在**服务端鉴权函数地址**填：

```
https://zgkovjgkkvxoajcqxwfw.supabase.co/functions/v1/admin-guard
```

点「启用」，然后用 `ADMIN_TOKEN` 里的口令登录。

**启用成功的标志**：页面顶部的红色警告消失，变成「🔒 服务端鉴权已启用」。

---

## 现在挡住了什么

| 攻击方式 | 结果 |
|---|---|
| 拿到链接直接打开 | 口令在服务端，答不上来进不去 |
| 暴力试口令 | 连错 5 次锁定 10 分钟，且每次失败都写入审计 |
| 抄走会话串到别的设备 | 会话绑定来源指纹，异地即作废 |
| 抄走 key 直接调数据库 | 已做的话：执行 `安全加固_数据库权限.sql` 收回权限 |
| 搜索引擎收录后台地址 | 已加 noindex |

---

## 还有一件事建议一起做

`安全加固_数据库权限.sql`（上一轮给你的那份）会：

- 确认匿名访客**没有删除权限**（否则一条请求清空全部档案）
- 只放开必要列的更新（`emp_id`、`created_at` 不给改）
- 提供一个**不含口令哈希**的只读视图

这份会动到游戏本体用的表，**建议先在后台确认名录能正常读出**再执行。

---

## 边界（必须知道）

**游戏本体仍用 anon key 直连数据库。** 这是因为注册、登录、云存档都必须由玩家浏览器直接发起，纯前端架构下没有第二选择。

所以严格讲：**能读到什么，取决于数据库的权限策略，而不是页面。**

要彻底解决，得把游戏本体的读写也迁到 Edge Function——那是架构改造，不是一次配置的事。现在的方案至少保证：**后台这扇门，是锁上的。**
