# 权路巅峰 · 云端存档部署指南

代码已完成，**未配置时游戏与接入前完全一致**，玩家无任何感知。

你需要做的只有三件事：建项目 → 建表 → 填两个值。

---

## 第一步：创建 Supabase 项目

1. 打开 `supabase.com`，用 GitHub 或邮箱注册
2. New project，填项目名、数据库密码
3. **Region（区域）选择**：
   - 若面向国内玩家，选 **Southeast Asia (Singapore)** —— 国内访问最快的区域
   - 没有中国大陆节点可选，这是现状
4. 等待约 2 分钟初始化

---

## 第二步：建表

1. 左侧 **SQL Editor** → New query
2. 打开本目录的 `schema.sql`，全选复制，粘贴进去
3. 点 **Run**

会创建三张表：

| 表 | 用途 |
|---|---|
| `game_users` | 用户档案（工号、姓名、联系方式、口令哈希、状态、登录次数） |
| `game_saves` | 存档数据（jsonb）+ 摘要字段（供后台列表直接展示） |
| `admin_logs` | 操作留痕，**只允许追加，不允许删除** |

同时开启行级安全（RLS）并配好策略。

---

## 第三步：填配置

1. 左侧 **Project Settings** → **API**
2. 复制两个值：
   - **Project URL** → 填 `url`
   - **anon / public** key → 填 `key`
   - ⚠️ 注意是 `anon`，**不是** `service_role`（后者会绕过 RLS，绝不能放前端）

3. 打开 `game.html` **最顶部**的配置块（约第 676 行前的 `<script>` 块，不参与压缩，方便直接改）：

```js
var CLOUD_CONFIG = {
  enabled: true,                              // ← 改 false 为 true
  url: "https://你的项目.supabase.co",         // ← 填 Project URL
  key: "eyJhbGciOi..."                        // ← 填 anon key
};
```

4. 重新构建并部署：
```bash
node build.js && bash push_curl.sh
```

---

## 后台入口

部署后访问：

```
https://你的用户名.github.io/quanli-dianfeng-game/admin.html
```

首次进入需设置管理员口令（存在本机浏览器）。

功能：档案名录、状态处置（冻结/恢复/注销）、重置口令、操作留痕、延迟自检。

---

## ⚠️ 国内访问：必须知道的事

**Supabase 没有中国大陆节点。** 实测数据：

| 来源 | 实测结果 |
|---|---|
| 腾讯云开发者实测 | PostgREST 接口 **700ms+**，Edge Function **1.5s+** |
| 21YunBox（阿里云张家口探测） | supabase.com 加载中位数 **5.2 秒** |
| 多篇测评 | 延迟 **250–800ms** 波动，晚高峰跨境链路易断流 |

**我的应对设计**（已实现）：

- 本地 localStorage 始终是**权威**，云端只是尽力而为的副本
- 所有云调用异步，失败**静默降级**，绝不阻塞游戏
- 上传节流 60 秒，避免频繁跨境请求拖垮体验
- 未上传的进度进入待同步队列，联网后自动补传
- 顶栏显示状态：`☁️ 已同步` / `⚠️ 离线（已存本机）`

**结论**：玩家能正常玩，只是同步可能慢或偶发失败。进度不会丢。

---

## 三个替代方案

如果延迟不可接受：

| 方案 | 延迟 | 合规 | 成本 | 说明 |
|---|---|---|---|---|
| **Supabase 官方云** | 250–800ms | 数据在境外 | 免费 / $25月 | 已完成，可直接用 |
| **火山引擎 Supabase** | 低 | 数据在国内 | 按量 | 国内托管版，兼容 Supabase 生态，需另注册 |
| **自建 Supabase** | 取决于服务器 | 自己可控 | 服务器费 | Docker 部署，运维成本高 |

三个方案**前端代码完全一样**，只需换 `url` 和 `key`。

---

## 安全须知

1. **只用 anon key**。service_role key 会绕过所有 RLS，放到前端等于把数据库交给所有人。
2. **后台口令是前端校验**——纯前端页面无法真正保密。要严格鉴权需收紧 RLS 或改用 Edge Function + service_role。
3. **anon key 会暴露在页面源码里**，这是 Supabase 的设计（靠 RLS 保护）。当前 RLS 已限制为"可读可插自己"，若需更严，在 Supabase 控制台调整策略。
4. 若收紧 RLS 导致后台操作失败，会**明确提示失败原因**，不会静默改坏数据。

---

## 常见问题

**Q：玩家不配置就能用吗？**
A：能。`CLOUD_CONFIG` 由你一次性填入，所有玩家共用同一个云端档案库。

**Q：老玩家本地已有进度，会丢吗？**
A：不会。登录时双向比对，用"游戏内年月"判定，取较新的一份。

**Q：换手机进度能找回吗？**
A：能，前提是原设备联网同步过至少一次。

**Q：免费项目 7 天不活跃会暂停？**
A：是的，Supabase 官方政策。300 人规模建议升 Pro（$25/月）避免暂停。

**Q：怎么关掉云端？**
A：把 `enabled` 改回 `false` 即可，游戏立刻退回纯本机模式。
